# Environment variables for ABIM Bias Checker deployment

# Path to the trained model directory
# This should point to the directory containing adapter_config.json and adapter_model.bin
MODEL_PATH=/app/ABIM_Bias_Checker_Results/RoBERTa_Optimized

# Flask Configuration
FLASK_ENV=production
FLASK_APP=src.app:app

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
